# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# AI coding-agent tool state (local machine config / installed skill packages,
# not part of the site's product source; reproducible via skills-lock.json)
.claude/
.agents/
.codex/

# Impeccable local-only config (build-path choice etc. is machine-specific)
.impeccable/config.local.json

# Impeccable working directories (QA screenshots, comps, build state)
.impeccable/review/
.impeccable/mocks/
.impeccable/build/

# One-off Lab content migration scripts (Velog/Notion -> Firestore/S3) —
# local tooling, not part of the shipped site
/scripts/migrate/
